{% extends "MakeupBundle::layout.html.twig"%}
{% block body %}

    <section class="main-content">
        <div class="padding">
            <div class="container">
                <div class="row">
                    <div class="col-sm-8">
                        <div class="list-posts">
                            {% for product in pagination %}
                                <article class="post type-post media">
                                    <div class="entry-thumbnail media-left pull-left"><img src="{{ product.image }}" alt="Image"></div><!-- /.entry-thumbnail -->
                                    <div class="entry-content media-body">
                                        <h2 class="entry-title"><a>{{ product.name }}</a></h2><!-- /.entry-title -->
                                        <p>
                                            {{ product.description }} 
                                        </p>
                                        <br>
                                        <h4>Tractament</h4>
                                        <p>
                                            {% for pt in product.productTreatment %}
                                                {{pt.treatment.name}}
                                            {% endfor %}
                                        </p>
                                        <br>
                                        <h4>Comentari</h4>
                                        <p>
                                            {{ product.comment }}
                                        </p>
                                    </div><!-- /.entry-content -->
                                </article><!-- /.post -->
                                <div class="clearfix"></div>
                            {% endfor %}
                        </div>

                        <div>
                            {{ knp_pagination_render(pagination) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}